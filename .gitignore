# ── Entorno virtual ──
venv/

# ── Python ──
__pycache__/
*.pyc
*.pyo
*.pyd

# ── IDE / Editor ──
.vscode/
.idea/
*.swp
*.swo

# ── Build (PyInstaller) ──
dist/
build/
*.spec

# ── Portable (generado con build.bat) ──
portable/*.exe

# ── Configuración local (máquina-específica) ──
config.json

# ── Pesos de modelos (descarga externa) ──
*.pt
models/

# ── Logs ──
logs/

# ── Sistema operativo ──
Thumbs.db
.DS_Store
